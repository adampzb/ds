version: "3.8"
services:
  web:
    build: .
    ports:
      - "8000:8000"
    environment:
      - ALLOWED_HOSTS=localhost,127.0.0.1,51.15.115.36
      - DJANGO_SETTINGS_MODULE=discussit.settings
      - ENVIRONMENT=production
      - DEBUG=False
      - SECRET_KEY=your-secure-secret-key-here-change-this-in-production
      - CORS_ALLOWED_ORIGINS=http://51.15.115.36,http://localhost:4200
      - CSRF_TRUSTED_ORIGINS=http://51.15.115.36
    command: gunicorn --config gunicorn_config.py discussit.wsgi:application
    volumes:
      - .:/usr/src/app
